# Pinned runtime dependencies for Streamlit Community Cloud and `pip install -r`.
# Full transitive lock: uv.lock (regenerate with `uv lock`; sync pins here to match).
# Requires Python >= 3.12.
streamlit==1.63.0
numpy==2.5.2
scipy==1.18.1
soundfile==0.14.0
matplotlib==3.11.1
pandas==3.0.5
altair==6.2.2
pyyaml==6.0.3
